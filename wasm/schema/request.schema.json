{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/luismascotto/subtitle-sanitizer/wasm/request.schema.json",
  "title": "subtitle-sanitizer WASM request",
  "type": "object",
  "required": ["subtitle"],
  "properties": {
    "subtitle": {
      "type": "string",
      "description": "UTF-8 subtitle source (SRT or ASS text). Use subtitleB64 if JSON escaping is awkward."
    },
    "subtitleB64": {
      "type": "string",
      "description": "Standard base64 of subtitle bytes; used if non-empty (overrides subtitle)."
    },
    "config": {
      "description": "Rules JSON (same shape as config.json). Omit, null, or {} for built-in defaults.",
      "type": ["object", "null"]
    }
  },
  "additionalProperties": true
}
