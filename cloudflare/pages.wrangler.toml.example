# Static Pages only — no Worker. Build output = web/wasm-demo/ (see cloudflare/README.md).
# Deploy: npx wrangler pages deploy web/wasm-demo --project-name=subtitle-sanitizer-wasm-demo
name = "subtitle-sanitizer-wasm-demo"
compatibility_date = "2025-01-01"
